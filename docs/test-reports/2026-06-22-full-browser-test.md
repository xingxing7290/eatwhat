# 2026-06-22 完整浏览器功能测试报告

## 测试范围
- 测试地址：http://113.44.50.108:3002
- 分支：codex/complete-couple-food-system
- 测试方式：Chrome 自动化浏览器 + 后端接口校验 + 容器状态检查
- 测试账号：自动创建短用户名测试账号，测试完成后已清理

## 测试中发现并修复的问题
1. 公共入口仍使用旧前端构建，导致新功能页面无法进入且页面文字看起来像乱码。
   - 已在服务器执行 `npm run build` 重新生成 `/srv/eatwhat/client/dist`。
   - 已重启 `eatwhat-frontend` 和 `eatwhat-nginx`。
2. Nginx 静态响应缺少明确字符集。
   - 已在 `nginx.conf` 和 `client/nginx.conf` 增加 `charset utf-8;`。
   - 复核 `Content-Type: text/html; charset=utf-8`。

## 通过的功能项
- 申请账号页面创建用户 A。
- 登录进入仪表盘，并写入登录 token。
- 使用邀请码创建用户 B，并确认 B 加入同一个小家。
- 默认菜品状态：默认总数 407，当前小家已导入 407，缺失 0。
- 菜品接口：`/meals?limit=500` 返回正常，当前分页取到 200 条，抽样图片字段不缺失。
- 想吃清单：B 创建高优先级想吃项，A 投票后 partnerSignal/votedByMe 正常。
- 周计划：生成 7 天草稿，应用到日历成功。
- 购物清单：从排餐生成、手动新增、勾选、清空已购均成功。
- 饭后回忆：PNG 图片上传成功，并能在饭桌相册接口中查到。
- 纪念日菜单：模板返回 5 个，创建纪念日菜单成功。
- 图片纠错：创建“图片不对”记录并标记 fixed 成功。
- 桌面端路由：仪表盘、日历、周计划、购物清单、菜品、回忆、相册、想吃、纪念日、设置均正常渲染。
- 菜品页：搜索“鸡蛋”、图片加载、详情页“做过一次 / 图片不对”按钮均正常。
- 移动端：仪表盘、日历、菜品、购物清单、设置页面在 390x844 视口正常渲染。
- 退出登录：设置页退出后回到登录页。

## 性能和错误情况
- 浏览器控制台错误：0
- 请求失败：0
- 后端 5xx：0
- 页面加载最慢：日历约 1.376 秒
- API 最慢：默认菜品导入检查约 0.794 秒
- 结论：没有发现明显操作卡顿；主要页面加载都在 1.5 秒以内。

## 构建与运行状态
- `npm run build` 通过。
- Docker 服务健康：MongoDB、backend、frontend、nginx 均运行正常。
- 已清理自动化测试产生的测试小家、测试账号和业务数据。

## 非阻塞提示
- Vite 构建仍提示主 chunk 超过 500KB，后续可做路由级拆包优化首屏加载。
- Sass legacy JS API 是依赖层警告，不影响当前运行。
