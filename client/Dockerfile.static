# 前端静态文件Dockerfile - 使用预构建文件
FROM nginx:alpine

# 复制预构建的静态文件
COPY dist/ /usr/share/nginx/html/

# 复制nginx配置文件
COPY nginx.conf /etc/nginx/nginx.conf

# 暴露端口
EXPOSE 80

# 启动nginx
CMD ["nginx", "-g", "daemon off;"] 